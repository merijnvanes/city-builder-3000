# Iteration 2 integration contract

Absolute root /Users/merijnvanes/CodeProjects/civic3000. Vanilla JS/Vite, Canvas2D. Existing user-added pnpm files must remain untouched. Do not commit/push. Three Claude workers maximum; do not delegate further. Use apply_patch for source edits.

## Simulation worker ownership
Own src/sim.js, src/infrastructure.js, optional src/economy.js and tests/v2-simulation.test.js ONLY. Maintain existing exports and 40x40 size, all old tool IDs. Extend TOOLS entries with group ('zone','transport','utilities','civic','landscape','demolish') and add tool IDs powerline,pipe,school,hospital,landfill,bus,rail. Standard tile type remains existing ground type (powerline/pipe must instead set tile.powerline/tile.pipe booleans and may overlay roads/zones; no demolition side effects). Tile density 1|2|3; level 0..4 maintained; levels capped density1=1,density2=2,density3=4. createCity starter attractive and financially sustainable for at least 10 years untouched; diversified density, less all-tower starter. New town blank starts $50000. place(city,x,y,tool,options={density:1}) returns {ok,message}; zoning occupied SAME zone may change density with cost but never free bulldoze. Power/pipe tools explicit networks; maintain backward compatibility old cities by migration seeding connecting utility routes under existing roads (legacy saves must load). New cities need new network rules; low density can develop with power+roads without water, medium/high requirewater. All buildings need road access for jobs/residentoccupancy. Add setPolicy(city,key,value) returns {ok,message} handling keys tax.residential/commercial/industrial, funding.police/fire/health/education/transport, ordinance.recycling/cleanAir/neighborhoodWatch, loan (value amount 10000), repayLoan. city.taxes:{residential,commercial,industrial}; oldcity.tax should remain supported fallback until migrated. city.funding percentages default100; city.ordinances booleans false; city.debt number; city.name 'New Riverton'. Stats preserve current keys, add unemployment,pollution,crime,health,education,garbage,traffic (0..100; unemployment percent, health/education higherbetter),debt,loanPayment,taxes,funding,ordinances,advisors array {name,role,mood:'good'|'warning'|'bad',message},news string[], history >=120months bounded240. Tile pollution/crime/traffic/landValue 0..100 for overlays; service coverage local and funding-dependent. Demand signed -100..100 tied to jobs/housing balance, taxes, services, decline under negative demand, preserve chance deterministic. Generated tiles extra flags and fields validated/migrated in save. Keep existing tests meaningful: tests relying old roads carry utilities may need change by parent; don't edit older tests. Add own tests proving mechanics. Aim compact robust implementation; avoid unrelated speculative complexity.

## UI worker ownership
Own src/ui.js, src/style.css, index.html ONLY. Rewrite classic city-builder interface: game fills viewport; compact metallic-gray/teal navy controls and inset/beveled borders; grouped right tool dock width180 desktop, lower-right navigator controls; bottom info bar and scrolling news; small top-left city title and menu controls; keep generous world space, NO cream dashboard-card layout. Original icons. Tool groups expandable submenu; RCI zones with density1/2/3 buttons; costs/readable tooltips; single inspect/bulldoze buttons; undo; explicit powerline and pipe tools. Mobile responsive tool dock collapsible, budget/advisor dialogs accessible. mountUI(actions) returns existing API {update,notify,setTool,setSelection,setOverlay} plus optional setBuildPreview({count,cost,valid,message}),setDensity(n),setSpeed(n). actions preserve existing and add setDensity(n),undo(),setPolicy(key,value),renameCity(name),toggleSound(),rotate(delta),setDisaster(id). Existing callback newCity(options?) default blank; actions newCity({scenario:'sandbox'|'growth'|'recovery'}) optional parent can implement. Add functional dialogs: Budget with separate RCI tax sliders, funding sliders, income/expenses/balance, loan/repay; City report with history SVG graph (no external deps) and stats; advisors with messages; ordinances toggles; help explaining rectangle zone drag, mouseupcommit, Escape cancel, Ctrl/CmdZ undo. data-tool attrs on actual clickable tools; data-density, data-speed, data-overlay. Initial speed0. Preserve #city-canvas #viewport #app and import src/main.js. Canvas touch-action:none; empty UIareas pointer-events:none. Add modal aria-labels and button aria-labels. No inline globalkeydown listener. No simulation loop. Import TOOLS fromsim dynamically categories fallback for early build. Overlay IDs none,power,water,landvalue,pollution,crime,traffic. Async data updates must not overwrite active sliders or rebuild focused elements. Include news ticker, RCI signed bar chart in bottom strip, money pop date. Project feels substantial and classic, not generic SaaS.

## Construction worker ownership
Own src/construction.js and tests/construction.test.js ONLY. Pure JS no DOM. Export planConstruction(city,start,end,tool,{density=1}={}) => {tiles:[{x,y,valid,cost,message}],cost,count,valid,message,tool,density}. Zone rectangle inclusive forresidential/commercial/industrial, parks/landfill rectangle; road/rail/powerline/pipe Manhattan L-path (dominant axis first) inclusive. Others single tile end. Hard bound coordinates 0..size-1 and max1600; malformedargs return invalidplan. Compute eligibility/cost without mutating city, same-zone density rezoning allowed. Use TOOLS cost for base, zone cost * density, powerline/pipe samebase; tool already present skip zero cost/noop rather than charge; groundwater onlyroad,rail? rail disallowwater. Never overwrite occupied unless bulldoze; pipes/lines overlay grass/road/zones but avoid water unlessroadbridge. Bulldoze cost max? effectivefee minus25%refund may negative, totalcost must sum true net impact; allow salvage financing consistently sim. Export applyConstruction(city,plan) => {ok,message,changed,cost}; validate/replan against current state and funds before anything commits, use place(...options); invalidtiles skip? contract: all eligible tiles commit atomically, blocked tiles shown red and skipped; if noeligible false; insufficientfunds false nochange. Must rollback entire city if unexpected place failure by serialize/deserialize or structuredClone restoring object keys (keep referencecity). Export createUndoManager(limit=20) => {record(city),undo(city):boolean,clear(),get size}; snapshot allsimulation fields, mutations restore cityinplace and bump revision to force cache. Integration parent records city immediatelybefore commit, undo policy actions optional. No UI/render edits. Read sim source carefully. New sim updates concurrently; basecostdensity API asabove. Tests independent scenarios including blockedcells, negativecoords, fundsshort, cancellation (no mutationplan), pathconnected, batchatomic rollback, undo. Avoid external dependencies.

## Parent ownership
src/main.js renderer.js minimap.js/css, new building-art.js/audio.js/scenarios.js, integration browser tests, documentation. Parent adds richer procedural architecture/shading street details, construction preview, rotation and correct picking; connects UI actions and worker modules, runs full tests and corrects integration. No other worker edits these.
